# 2026-04-07 — LOD Daily Notes

## Issue #22 Investigation — xform GetTypedValue Crash

### Background
- hdLod scene index plugin was crashing when reading flattened xform data sources
- Commit 52e98b2 added null-checks to `_MatrixCombinerDataSource` as a fix
- Units (agent) suggested the crash was caused by PR #8's purpose-visibility changes

### Vanilla USD Build Completed
- `/tmp/usd-vanilla-build/` — stock OpenUSD release v26.03 (no PR #8 changes)
- Built with `build_usd.py --no-python` (Python 3.10)
- Includes xformReadTest plugin (minimal scene index that reads xforms)

### Definitive Test Results — Vanilla USD v26.03
- `GetTypedValue(0.0f)` in `_PrimsAdded`: **SUCCESS** — pos=(0.00, 1.50, 5.00) ✅
- `GetTypedValue(0.0f)` in `_PrimsDirtied`: **SUCCESS** — pos=(0.00, 0.00, 0.00) ✅
- UsdView ran without crash, timeout after 25s (exit 124, not segfault)
- Plugin loaded via LD_PRELOAD (vanilla build's plugin registry didn't auto-load hdLod without it)

### ABI Mismatch Discovery
- Fork build's UsdView was crashing at `_ToRendererCreateArgs` → null function pointer (0x0)
- GDB backtrace: Thread 1 crash in `HdRendererPlugin::CreateRenderer()`
- Root cause: **partial rebuild** of `libusd_hd.so` (Apr 7 03:16) while downstream libs (`libusd_usdImaging.so` etc.) were from Apr 5
- The fork's `poc/lod-proposal` branch has significant changes to `hd/dataSource.h`:
  - New APIs: `HdVisitSampledDataSourceType`, `HdCopySampledDataSourceType`
  - HD_API_VERSION bumped 95→97
  - Scene index plugin ordering changed to Hybrid mode with cycle detection
- Only `libusd_hd.so` and `libusd_hdSt.so` were rebuilt; all other libs expect old ABI
- **This was NOT the xform issue — it was a build artifact**

### Clean Full Rebuild In Progress
- `/tmp/usd-fork-clean-build/` — full rebuild of fork (`poc/lod-proposal` branch)
- Reverted commit 52e98b2 (null-check fix) to test original upstream code path
- Added xformReadTest plugin alongside hdLod for diagnostic output
- Build started at ~04:48 UTC, expected ~15 min
- **Goal**: determine if issue #22 is a genuine bug in PR #8 or was always the ABI mismatch

### Key Lesson
- **Never partially rebuild USD** — ABI changes in `hd/` require full rebuild of all downstream libs
- The HD_API_VERSION bump (95→97) in the fork's `hd/dataSource.h` was the smoking gun

## hdLod Live UsdView Demo — Blocked

### What Works
- hdLod auto-loads via Hybrid plugin ordering (no LD_PRELOAD needed) ✅
- Stage captured, groups/items cached, camera discovered ✅
- Hydra `GetTypedValue(0.0f)` reads camera xform correctly ✅
- LOD evaluates correctly at frame 1: 2 hidden, 2 dirty, cam=(0,1.5,3) ✅

### What Doesn't Work
- UsdView playback (`_playClicked()`) does NOT trigger `_PrimsAdded`/`_PrimsDirtied` on each frame
- `usdviewApi.frame` is **read-only** in from-source builds (`can't set attribute 'frame'`)
- Plugin evaluates once on load, never re-evaluates during animation scrubbing

### Root Cause
UsdView's time-change mechanism doesn't send per-frame notifications through the scene index chain the way we expected. The `SetTime()` call updates the stage scene index but may not generate `PrimsAdded` entries for camera prims (which aren't "added" — they just change position).

### Possible Fixes
1. Listen for `UsdNotice::ObjectsChanged` on the stage and re-evaluate when time changes
2. Hook into `_PrimsDirtied` with broader locator matching (not just camera path)
3. Register for time-change callbacks via the UsdImagingStageSceneIndex API

### Deliverables Completed
- Pre-baked demo GIF: 50 frames, LOD switching at correct thresholds, rendered via usdrecord ✅
- Committed to both repos ✅
- Issue #22 closed with full investigation ✅

### Commits Today
- `cc052c0` — Revert _MatrixCombinerDataSource null-check, use Hydra xform reads
- `313b2fb` — hdLod: re-evaluate LOD on PrimsAdded (frame scrub support)
- `b52f798` — Add Phase 2 hdLod demo GIF and clean scene (usd-lod-poc)
